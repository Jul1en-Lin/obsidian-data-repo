# String类型
> 相关笔记：[[Redis|Redis 知识总结]]


# 命令

![image](image-20260410232539-vq0kx0p.png)

![image](image-20260410232613-jk00oun.png)

![image](image-20260412225043-1nr0uzl.png)

![image](image-20260412225128-8o1qppz.png)

注意，redis的区间是左闭右闭的

# String 编码方式

1. int

   8字节的整数
2. embstr

   压缩字符串，适用于比较短的字符串。有时会根据业务场景需要，将长的字符串也压缩为embstr格式的
3. raw

   普通字符串，适用于更长的字符串，只是单纯的持有字节数组

可以通过 `object encoding key`命令查看编码方式

![image](image-20260412225713-y98rr7p.png)

# 应用场景

1. 作为缓存功能，提供中间件角色的服务

   ![image](image-20260412225913-4pdk27k.png)

2. 实现快速计数功能，同时数据可以异步处理落地到其他数据源，用于统计

   之所以用到异步是因为像MySQL这样的数据源它写入速度没有Redis那么快，而且数据源没有立刻写入的必要性，可以慢慢写

   ![image](image-20260412230122-qm0eacw.png)

3. 共享会话（Session）

   一般情况来说，负载均衡，用户请求可能会被负载均衡分配到不同的服务器中处理，如果没有Redis，用户与服务器之间的session就会被独立存储，这就很有可能用户登录页面后，刷新页面登录信息没了，要重新登录，想要维持每次用户都要维持在同一个服务器上，正确读到session，除了可以通过技术手段，让某一个用户只给特定服务器处理当前session，还有一个办法就是共享session。

   ![image](image-20260412230650-mhi49kh.png)

   将Session共同存进Redis中，用户无论访问到哪个服务器上，服务器都要从Redis中取出Session，用户之前的各种信息就不会丢了

   ![image](image-20260412230725-xygrndk.png)

4. 验证码的临时存储

   <u>联想到抽奖项目中的邮箱验证码登录功能：设置过期时间和校验功能。</u>
